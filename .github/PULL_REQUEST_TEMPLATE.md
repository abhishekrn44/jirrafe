**What this changes**

**Why**

**Checklist**

- [ ] A test fails before and passes after (fixture change if an extractor rule changed)
- [ ] `./gradlew build` is green; `cd jirrafe-maven && ./mvnw -B install` if the Maven plugin changed
- [ ] Node ids and JSON shapes are unchanged, or the change is called out here
